import { connect } from 'react-redux'
import Pursuit from '../components/Pursuit'

export const mapStateToProps = () => ({
})

export const mapDispatchToProps = {
}

export default connect(mapStateToProps, mapDispatchToProps)(Pursuit)
